var ListNode = require('../../structures/ListNode');
var buildTree = require('../../structures/TreeNode');

module.exports = [
  {
    input: ["Bob hit a ball, the hit BALL flew far after it was hit.", ["hit"]],
    output: 'ball'// ListNode.generateList(807),
  }
];